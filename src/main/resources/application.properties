spring.application.name=ConcertCrew

# H2 in-memory database
spring.datasource.url=jdbc:h2:mem:concertcrewdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=false
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# JWT
app.jwt.secret=${JWT_SECRET:ConcertCrewSecretKey2026ChangeThisInProductionToSomethingMuchLonger}
app.jwt.access-expiry-ms=1000000
app.jwt.refresh-expiry-ms=604800000

# Session cookie hardening (ZAP: SameSite)
server.servlet.session.cookie.same-site=lax
server.servlet.session.cookie.http-only=true

# Seed account passwords (externalized; override via env vars)
app.seed.admin-password=${SEED_ADMIN_PASSWORD:admin123}
app.seed.user-password=${SEED_USER_PASSWORD:user123}

# SpringDoc / Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha

# Server
server.port=8090

# Database backup
app.backup.dir=./backups

spring.jackson.deserialization.fail-on-null-for-primitives=false

server.servlet.session.tracking-modes=cookie
